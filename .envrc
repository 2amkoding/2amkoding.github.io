# Automatically load project-specific aliases when entering this directory
# Requires direnv: brew install direnv

# Load Jekyll project aliases
source_env_if_exists .aliases

# Set PROJECT_ROOT for convenience
export PROJECT_ROOT=$(pwd)

# Add local bin to PATH if it exists
PATH_add ./bin

echo "🏠 Entered Jekyll project directory: $(basename "$PWD")"
